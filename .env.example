# Zerodha Kite Connect Credentials
KITE_API_KEY=your_kite_api_key
KITE_API_SECRET=your_kite_api_secret
# Access token must be generated daily or via an automated script
ACCESS_TOKEN=your_access_token

# Database Connection
# For MongoDB Atlas: mongodb+srv://<username>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority
# For Local: mongodb://localhost:27017
MONGODB_URI=mongodb://localhost:27017
DB_NAME=stormalert

# Security
# Generate a secure random string for JWT signing
JWT_SECRET=super_secret_jwt_key_change_this
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Notification Services (Optional - Leave empty if not using)
# Twilio (WhatsApp)
TWILIO_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=whatsapp:+14155238886

# Telegram
TELEGRAM_BOT_TOKEN=

# Email (SMTP)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
